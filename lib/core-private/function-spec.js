export default class FunctionSpec {
    constructor(config) {
        this.name = config.name
        this.args = config.args
        this.ret = config.ret
        this.function = config.function
    }
}